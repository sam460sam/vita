// Widget bundle entry point. Add to the "GeodeWidget" extension target.
import WidgetKit
import SwiftUI

@main
struct GeodeWidgetBundle: WidgetBundle {
    var body: some Widget {
        GeodeWidget()
    }
}
